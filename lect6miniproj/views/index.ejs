<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="main w-full min-h-screen bg-zinc-900 p-8">
        <div class="form max-w-md mx-auto bg-zinc-800 p-6 rounded-lg shadow-lg">
            <form action="/create" method="post">
                <input class="block w-full rounded-md mt-2 mb-4 px-5 py-2 bg-zinc-700 text-white rounded" type="text"
                    placeholder="Title goes here" name="title">
                <textarea class="block w-full rounded-md mt-2 mb-4 px-5 py-2 bg-zinc-700 text-white rounded resize-none"
                    placeholder="Write your task details here" name="details"></textarea>
                <input class="w-full mt-2 px-5 py-2 bg-blue-600 text-white rounded cursor-pointer hover:bg-blue-700"
                    type="Submit" value="Create task">
            </form>
        </div>
        <div class="tasks flex gap-3 flex-wrap p-10">
            <%if(files.length> 0){ %>
                <% files.forEach(function(val){ %>
                    <div class="task w-72 px-3 py-4 rounded-md bg-zinc-800">
                        <h1 class="text-white text-3xl tracking-tighter"><%= val %></h1>
                        <a class="text-blue-600 inline-block mt-3" href="/file/<%= val %>">Read more</a>
                    </div>
                    <% })%>
                        <% } else { %>
                            <h3 class="text-zinc-600">No task available</h3>
                            <% } %>


        </div>
    </div>
    <!-- <script src="/javascripts/script.js"></script> -->
</body>

</html>